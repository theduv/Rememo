import Header from "../Header/Header";
import useDecksStore from "../../stores/decks";

const Settings = () => {
  return (
    <div>
      <Header title={"Settings"} />
      <h1 className="text-center">
        This page is not done yet. Come back later to see what changed!
      </h1>
    </div>
  );
};

export default Settings;
